# Production environment variables for Vite
# Do NOT hardcode VITE_API_BASE_URL here — the hostname-based logic
# in baseUrl.js handles prod vs test automatically.
# CI/CD can override via Docker env vars (window.__ENV__ in env.js).
