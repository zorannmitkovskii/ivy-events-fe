# Production environment variables for Vite
# This is used when building with `vite build --mode production`
# Set to your actual production backend URL. CI/CD can override via environment.
VITE_API_BASE_URL=https://api.example.com
